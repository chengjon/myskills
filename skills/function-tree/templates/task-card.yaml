task:
  id: "NODE-ID"
  title: "Short implementation title"

scope:
  allowed_paths:
    - "src/path/to/component"
  forbidden_paths: []

non_goals:
  - "Name at least one thing this task must not change"

acceptance:
  commit_gate:
    - "project build gate passes"
  closeout_gate:
    - "project test suite passes"

evidence:
  current_head: ""
  notes: []
